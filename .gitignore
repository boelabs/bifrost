# dependencies
node_modules/

# build output & caches
dist/
.next/
# Regenerated by every ; the dashboard already ignored its own copy.
next-env.d.ts
.turbo/
# Generated by fumadocs-mdx from content/docs; rebuilt on install and on every build.
.source/
coverage/
*.log
*.tsbuildinfo

# materialized extension modules (content-addressed runtime cache)
.extensions-cache/

# env (committed example only)
.env
.env.local
.env.*
!.env.example

# editor / os
.DS_Store
Thumbs.db
desktop.ini

# local AI tooling — personal settings, session state, and private operator notes.
# AGENTS.md is the shared, committed agent contract; *.local.md holds anything that
# must not leave this machine (production hosts, runbooks, credentials handling).
.claude/
.codex/
.agents/
.cursor/
.aider*
.windsurf/
.gemini/
CLAUDE.local.md
AGENTS.local.md
.mcp.local.json

# agent scratch space (use for throwaway scripts, notes, and reports)
.scratch/

# legacy / binary lockfiles (bun.lock IS committed)
bun.lockb
